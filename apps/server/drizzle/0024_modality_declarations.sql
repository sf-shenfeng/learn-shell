-- 脑图裁量条款子项 — 脑图缺席要留言。
--
-- 产品原则: 脑图是可选教具(现有验尺已如此), 但允许缺席, 不允许沉默的缺席——
-- 不配脑图必须留一句教学法理由, 理由会被验尺输出展示。理由落在 lessons 行
-- 本身上(不是关课记录, 也不是回执七枚举——脑图缺席跟"这节课有没有关"是两件
-- 事, 备课阶段就该能声明, 不用等到关课)。可空——只有真正跳过脑图的课才写。
-- key 目前只有 mindmap 一个, 用 jsonb 而非单列是给以后其他可选教具(如脑图
-- 之外还可能出现的教具)留声明位, 不用每加一种教具就再迁移一次。
ALTER TABLE "lessons" ADD COLUMN IF NOT EXISTS "modality_declarations" jsonb;
